module.exports = function (app){

  var api = app.api.consulta;

  app.route ('v1/consultas')
//    .get(api.buscaPorId)
    //.delete(api.removePorId) a Implementar
    //.put(api.atualiza); a Implementar
}
